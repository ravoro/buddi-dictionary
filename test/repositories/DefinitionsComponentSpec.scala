package repositories

import org.scalatestplus.play.PlaySpec

class DefinitionsComponentSpec extends PlaySpec {
  "insertDefinitionRecordBatch" must {
    "insert nothing if the given value is empty" in {
      pending
    }

    "insert a new definition if one is given" in {
      pending
    }

    "insert multiple definitions if multiple are given" in {
      pending
    }

    "throw an exception when one of the given words already exists" in {
      pending
    }
  }
}
